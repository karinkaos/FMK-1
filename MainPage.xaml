<Page
    x:Class="FMK_1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FMK_1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="Pink">
        
        <Grid Background="Blue" Visibility="Collapsed">
            
        </Grid>
        
        <Grid Name="Start" Width="450" Height="600" Background="Gray">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!--<StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Width="50" Height="50" Margin="5"/>
                <Button Width="50" Height="50" Margin="5"/>
                <Button Width="50" Height="50" Margin="5"/>
                <Button Width="50" Height="50" Margin="5"/>
            </StackPanel>-->

            <Button Name="StartGameBtn" Grid.Row="1" Width="200" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center" 
                    Background="Red" Click="StartGameBtn_Click" Content="Start Button" FontSize="30"/>
        </Grid>
    </Grid>
</Page>